<div>
    <span class="content">Front: <strong><%= feed_item.card_front %></strong> | Back: <strong><%= feed_item.card_back %></strong></span>
  

  <div style= "float:right;">
<% user = Card.user rescue User.find(feed_item.user_id) %>
  <%= link_to "delete", feed_item, :method => :delete,
                                     :confirm => "You sure?",
                                     :title => feed_item.card_front %>
  </div>

</div>
<hr />
